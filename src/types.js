// @flow

export type ProductType = {
  id: string,
}

export type Product24HrStatsType = {
  id: string,
  open: number,
  high: number,
  low: number,
  last: number,
  volume: number,
  volume_30day: number,
}
